apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubedev-ingress-nginx.fullname" . }}
  labels:
    {{- include "kubedev-ingress-nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubedev-ingress-nginx.selectorLabels" . | nindent 4 }}
